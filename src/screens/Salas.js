import React from "react";
import { Text, View, StyleSheet, Touchable, TouchableOpacity } from "react-native";

export default function Perfil(){
    
    return(
        <View>
            <Text>ops, parece que não tem nenhuma sala aqui. Deve ter passado um furacão no senai, o que fez as salas sumirem. Ou só não programaram direito mesmo.</Text>
        </View>
    )
}